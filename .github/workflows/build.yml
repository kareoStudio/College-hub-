name: Build College Hub APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
      
      - name: Install Android Build Tools 34.0.0
        run: |
          export ANDROID_HOME=$ANDROID_SDK_ROOT
          yes | sdkmanager --licenses || true
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
      
      - name: Install Gradle 8.0
        run: |
          sudo rm -rf /usr/share/gradle-*
          wget -q https://services.gradle.org/distributions/gradle-8.0-bin.zip
          unzip -q gradle-8.0-bin.zip
          sudo mv gradle-8.0 /opt/gradle-8.0
          sudo ln -sf /opt/gradle-8.0/bin/gradle /usr/local/bin/gradle
          gradle --version
      
      - name: Install Cordova
        run: npm install -g cordova
      
      - name: Setup Cordova Project & Copy Files
        run: |
          # 1. Create project with matching package name
          cordova create college-hub com.kareostudio "College Hub"
          
          # 2. Copy config.xml and icon
          cp config.xml college-hub/config.xml
          mkdir -p college-hub/res
          [ -f icon.png ] && cp icon.png college-hub/res/icon.png || echo "icon.png not found, skipping"
          
          # 3. Copy index.html and main.html (agar redirect trick use kar rahe ho)
          cp index.html college-hub/www/index.html
          [ -f main.html ] && cp main.html college-hub/www/main.html || echo "main.html not present"
          
          cd college-hub
          cordova platform add android@13.0.0
          
          # 4. Plugins required for College Hub
          cordova plugin add cordova-plugin-inappbrowser
          cordova plugin add cordova-plugin-network-information
          cordova plugin add cordova-plugin-statusbar
          cordova plugin add cordova-plugin-file
          cordova plugin add cordova-plugin-filepath
          cordova plugin add cordova-plugin-filechooser
          cordova plugin add cordova-plugin-android-permissions
          
          echo "=== www files copied ==="
          ls -la www/
      
      - name: Build Debug APK
        env:
          ANDROID_HOME: ${{ env.ANDROID_SDK_ROOT }}
        run: |
          cd college-hub
          cordova build android --debug
      
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: College-Hub-APK
          path: college-hub/platforms/android/app/build/outputs/apk/debug/app-debug.apk
      
      - name: Upload to Release
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: softprops/action-gh-release@v2
        with:
          files: college-hub/platforms/android/app/build/outputs/apk/debug/app-debug.apk
          tag_name: v1.0.0-${{ github.run_number }}
          name: College Hub v1.0.0 Build ${{ github.run_number }}
          body: |
            ## College Hub APK
            - App Name: College Hub
            - Package: com.kareostudio
            - Target SDK: 34 (Android 14)
            - Build Date: ${{ github.event.head_commit.timestamp }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
